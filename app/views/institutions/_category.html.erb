<li>
  <span class="icon"></span>
  <%= link_to truncate(category.name, :length => 20), root_url + "categorie/#{category.id}", :class => "label" %>
  <span class="statometru"><% category.rate %></span>
</li>
